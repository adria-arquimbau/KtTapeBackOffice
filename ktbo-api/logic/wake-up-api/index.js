const { models: { Article } } = require('ktbo-data')

/**
 * Wake Up the Api
 * 
 * @returns {Promise} - Returns a Promise with a OK message.
 */

module.exports = function () {

    return(async () => { 
      return
    })()
}